import { SetClassDirective } from './fix-table-header.directive';

describe('SetClassDirective', () => {
  it('should create an instance', () => {
    const directive = new SetClassDirective();
    expect(directive).toBeTruthy();
  });
});
