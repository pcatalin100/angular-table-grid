import { SelectRowDirective } from './select-row.directive';

describe('SelectRowDirective', () => {
  it('should create an instance', () => {
    const directive = new SelectRowDirective();
    expect(directive).toBeTruthy();
  });
});
