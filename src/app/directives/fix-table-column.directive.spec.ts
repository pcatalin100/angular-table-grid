import { FixTableColumnDirective } from './fix-table-column.directive';

describe('FixTableColumnDirective', () => {
  it('should create an instance', () => {
    const directive = new FixTableColumnDirective();
    expect(directive).toBeTruthy();
  });
});
