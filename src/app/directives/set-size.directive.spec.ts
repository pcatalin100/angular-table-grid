import { SetSizeDirective } from './set-size.directive';

describe('SetSizeDirective', () => {
  it('should create an instance', () => {
    const directive = new SetSizeDirective();
    expect(directive).toBeTruthy();
  });
});
